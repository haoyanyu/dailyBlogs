import React from 'react';

interface IProps {
  
}
const CompontentName: React.FC<IProps> = (props) => {
  const {params} = props;
  
  return (
    <div>dashboard element</div>
  )
}

export default CompontentName;
